---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: build-image
    image: plugins/docker
    settings:
      username: sernafa
      password: mi9Feeat!
      repo: sernafa/docker-syspass
      tags: 3.1.0

